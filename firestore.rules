rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Explicitly allow all operations for development prototype
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}