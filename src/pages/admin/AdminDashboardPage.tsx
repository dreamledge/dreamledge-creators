import { SectionHeader } from "@/components/ui/SectionHeader";

export function AdminDashboardPage() {
  return <SectionHeader eyebrow="Admin" title="Moderation and competition control room" description="Prototype admin tools for contests, reports, featured content, and battle disputes." />;
}
